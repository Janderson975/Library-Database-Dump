This repository exists to organize and display an SQL Database that represents a small digital library. 
The database was designed on PostgreSQL but should work on all standard SQL software.
Queries.txt is a text file of test queries one can run to understand the database.
